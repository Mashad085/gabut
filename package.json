{
  "name": "community-finance",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:worker\"",
    "dev:frontend": "cd apps/frontend && npm run dev",
    "dev:backend": "cd apps/backend && npm run dev",
    "dev:worker": "cd apps/worker && npm run dev",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "install:all": "npm install && cd apps/frontend && npm install && cd ../backend && npm install && cd ../worker && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
